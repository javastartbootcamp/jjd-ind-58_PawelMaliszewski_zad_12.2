package employee;

public class Support extends Employee {
    public Support(String firstName, String lastName, String pesel, double wages) {
        super(firstName, lastName, pesel, wages);
    }
}
