package employee;

public class Management extends Employee {
    public Management(String firstName, String lastName, String pesel, double wages) {
        super(firstName, lastName, pesel, wages);
    }
}
